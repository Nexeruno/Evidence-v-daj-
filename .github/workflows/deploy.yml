name: 🚀 Deploy & Backup

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy-pages.outputs.page_url }}

    steps:
      - name: 📥 Checkout code
        id: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 Setup Node.js
        id: setup
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        id: install
        run: npm ci
        continue-on-error: false

      - name: 🔍 Run linter
        id: linter
        run: |
          echo "▶️ Starting ESLint..."
          npm run lint
          if [ $? -eq 0 ]; then
            echo "✅ Linter passed"
          else
            echo "❌ Linter failed"
            exit 1
          fi

      - name: 🧪 Run unit tests
        id: tests
        run: |
          echo "▶️ Starting unit tests..."
          npm test
          if [ $? -eq 0 ]; then
            echo "✅ All tests passed"
          else
            echo "❌ Tests failed"
            exit 1
          fi
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          VITE_ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}

      - name: 🔨 Build application
        id: build
        run: |
          echo "▶️ Building application..."
          npm run build
          if [ $? -eq 0 ]; then
            echo "✅ Build successful"
          else
            echo "❌ Build failed"
            exit 1
          fi
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          VITE_ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}

      - name: 🌐 Smoke tests (local)
        id: smoke
        run: |
          echo "▶️ Running smoke tests on local build..."

          # Check if dist/index.html exists and is not empty
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Build artifact not found: dist/index.html"
            exit 1
          fi

          echo "📄 Checking HTML structure..."
          if grep -q 'id="root"' dist/index.html; then
            echo "✅ Root div found in HTML"
          else
            echo "❌ Root div not found in HTML"
            exit 1
          fi

          # Verify HTML is valid (basic check)
          if grep -q '<html' dist/index.html && grep -q '</html>' dist/index.html; then
            echo "✅ HTML structure looks valid"
          else
            echo "❌ HTML structure incomplete"
            exit 1
          fi

          # Check build size
          BUILD_SIZE=$(du -sh dist | cut -f1)
          echo "📦 Build size: $BUILD_SIZE"

          if [ $(find dist -name '*.js' -o -name '*.css' | wc -l) -gt 0 ]; then
            echo "✅ Assets generated successfully"
          else
            echo "❌ No assets found in build"
            exit 1
          fi

          echo "✅ Smoke tests passed"

      - name: 📤 Upload Pages artifact
        id: upload-pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist'

      - name: 🚀 Deploy to GitHub Pages
        id: deploy-pages
        uses: actions/deploy-pages@v4

      - name: 🎉 Deployment successful
        id: success
        run: |
          echo "✅✅✅ DEPLOYMENT COMPLETE ✅✅✅"
          echo "📍 Deployed to: https://nexeruno.github.io/Evidence-v-daj-/"
          echo "⏱️ Completed at: $(date)"

      - name: 🚨 Notify on failure (Linter)
        id: notify-linter
        if: failure() && steps.linter.outcome == 'failure'
        run: |
          echo "❌ DEPLOYMENT FAILED: Linter check failed"
          echo "📋 Run 'npm run lint' locally to fix issues"
          exit 1

      - name: 🚨 Notify on failure (Tests)
        id: notify-tests
        if: failure() && steps.tests.outcome == 'failure'
        run: |
          echo "❌ DEPLOYMENT FAILED: Unit tests failed"
          echo "📋 Run 'npm test' locally to debug"
          exit 1

      - name: 🚨 Notify on failure (Build)
        id: notify-build
        if: failure() && steps.build.outcome == 'failure'
        run: |
          echo "❌ DEPLOYMENT FAILED: Build failed"
          echo "📋 Check build output above for errors"
          exit 1

      - name: 🚨 Notify on failure (Smoke tests)
        id: notify-smoke
        if: failure() && steps.smoke.outcome == 'failure'
        run: |
          echo "❌ DEPLOYMENT FAILED: Smoke tests failed"
          echo "📋 Build artifacts are corrupted or incomplete"
          exit 1
